[package]
name = "alice-persist"
version = "0.1.0"
edition = "2021"
description = "Persistence isolator — Document<T> and TextFile primitives."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
